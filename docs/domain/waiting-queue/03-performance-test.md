# 대기열 성능 테스트 (k6)

- 작성일: 2026-07-10
- 수정일: 2026-07-10 — 1차 실행 결과 기입, spike 30초 ramp-up 도입, loadtest 모니터링(redis_exporter + Grafana) 추가
- 스크립트: `k6/waiting-queue-load.js` (공통 헬퍼: `k6/lib/helpers.js`)
- 대상: `POST /api/v1/queue/enter` → `GET /api/v1/queue/position` 폴링 → `POST /api/v1/orders`(Entry-Token 검증) → `POST /api/v1/payments` → 결제 성공 시 토큰 삭제(`EntryTokenConsumeEventListener`)까지의 전체 흐름

## 1. 테스트 목적

1. **처리량 초과 안정성**: 스케줄러 배치(100ms당 20명, 초당 200명 발급)를 초과하는 요청이 들어와도 시스템이 안정적인지 확인한다.
2. **발급 TPS 적정성 판단**: 초당 200명 발급이 다운스트림(주문-결제)이 감당 가능한 수준인지 확인한다. 대기열의 존재 이유는 다운스트림 보호이므로, **주문-결제의 최대 안정 TPS보다 발급 속도가 높다면 배치 크기를 줄여야 한다.**
3. **전체 생명주기 검증**: 대기열 진입 → 토큰 발급 → 주문 → 결제 → 결제 성공 시 토큰 삭제(캐시 삭제)까지 부하 상황에서 정상 동작하는지 확인한다.

## 2. 테스트 전제 (시드 데이터)

| 항목 | 값 | 비고 |
| ---- | ---- | ---- |
| 유저 | 10,000명 | setup 단계에서 admin/일반 API로 생성 (`http.batch` 병렬 시드) |
| 상품 | 100개 | 브랜드 1개 (브랜드는 무관) |
| 재고 | 상품당 100,000,000 | 10,000명 전원 구매해도 충분 |
| 토큰 발급 | 100ms당 1명 (초당 10명) — 3차까지 20명, 4·5차 2명, 6차부터 1명 (ADR-041) | `EntryTokenPublishScheduler` |
| Entry-Token TTL | 5분 | Redis String |
| PG 시뮬레이터 | 요청 40% 실패, 1~5초 후 콜백 | 결제 실패 경로가 자연 발생 |

> 주의: 유저 10,000명 시드는 회원가입마다 서버가 BCrypt 해싱을 수행하므로 수 분이 걸릴 수 있다. `setupTimeout`을 30분으로 설정해 두었다.

## 3. 시나리오

### S1. 스파이크 + 폴링 부하 (`SCENARIO=spike`, 기본)

10,000 VU가 30초(`RAMP`, 기본 30) 동안 균등하게 대기열에 진입한 뒤, 각자 토큰을 받을 때까지 `/queue/position`을 폴링한다.

- **진입**: 초당 약 333명 `POST /queue/enter` — 발급 속도(초당 200명)를 계속 초과하는 유입.
- **왜 동시 진입이 아닌가**: 1차 실행에서 10,000명을 같은 순간에 투입하자 TCP 연결/Tomcat 스레드풀 한계로 서버가 연결 수락조차 못 했다(enter 타임아웃·dial i/o timeout 폭주, 11분간 46명 완료). 이는 대기열 로직이 아니라 접속 계층의 붕괴라 측정 의미가 없어, 현실적인 스파이크(짧은 시간 내 집중 유입)로 조정했다. `RAMP=0`으로 동시 진입 재현은 가능하다.
- **폴링 정책** (설계 문서 5-7과 동일):

  | 남은 순번 | 폴링 간격 |
  | ---- | ---- |
  | 5,000 초과 | 5초 |
  | 1,000 ~ 5,000 | 3초 |
  | 1,000 미만 | 1초 |

  (3차까지는 3/2/1초 — 발급 TPS 축소에 맞춰 4차부터 완화, ADR-041)

- 초당 200명 발급이면 대기열 소진에 약 50초가 걸리고, 그동안 폴링이 **초당 수천 회** 지속된다. 이 지속 폴링이 1회성 진입 스파이크보다 큰 실부하다.
- 토큰을 받은 유저는 곧바로 주문-결제 흐름(S3 포함)으로 진입한다.

### S2. 지속 유입 초과 (`SCENARIO=saturation`)

진입 속도(기본 초당 300명)가 발급 속도(초당 200명)를 계속 초과하는 상태를 유지한다.

- `constant-arrival-rate`로 초당 `RATE`명씩, 유저 10,000명이 소진될 때까지 진입 (기본 300/s → 약 33초 유입).
- 유입 종료 후에도 각 VU는 토큰을 받을 때까지 폴링을 계속하므로, **백로그가 쌓였다가 배출되는 전 과정**에서 enter/position 응답 지연이 안정적인지 관찰한다.
- 관찰 포인트: 대기열 길이 증가 중에도 `enter`/`position` p95가 유지되는가, `waitingCount`·`estimatedWaitSeconds`가 계속 유효한 값인가, Redis 메모리가 안정적인가.

### S3. 결제 실패 시 토큰 재사용 (S1/S2 흐름에 내장)

PG 시뮬레이터가 요청의 40%를 실패시키므로, 결제 실패 → 토큰 유지 → 동일 토큰으로 재주문 경로가 부하 중에 자연 발생한다.

- 결제 후 `/queue/position`을 최대 20초 폴링해 **404(토큰 삭제됨) = 결제 성공 + 캐시 삭제 완료**로 판정한다.
- 20초가 지나도 토큰이 남아 있으면(즉시 FAILED 또는 콜백 실패) **동일 토큰으로 주문-결제를 1회 재시도**하고 결과를 `token_reuse` 메트릭으로 집계한다.
- 검증 정책: 결제 실패 시 토큰은 유지되어 TTL(5분) 내 재주문에 사용할 수 있다 (요구사항 5-6).

## 4. 측정 지표

| 메트릭 | 의미 | 판정 기준 |
| ---- | ---- | ---- |
| `http_req_duration{name:enter}` | 대기열 진입 응답 시간 | p95 < 1s |
| `http_req_duration{name:position}` | 폴링 응답 시간 | p95 < 1s |
| `queue_wait_time` | 진입 → 토큰 발급까지 대기 시간 (P90/P95 포함) | 이론값(순번/발급 TPS) 대비 크게 벗어나지 않는가 |
| `journey_time` | 진입 → 결제 완료(토큰 소비 확인)까지 전체 여정 (P90/P95 포함) | 완주 유저의 종단 경험 시간 |
| `queue_waiting_count` | enter 응답의 waitingCount 추이 | 대기열 깊이 관찰 (스냅샷) |
| `flow_result` | 단계별 결과 분포 (TOKEN_CONSUMED / WAIT_TIMEOUT / ORDER_REJECTED_401 등) | TOKEN_CONSUMED 비율이 결제 성공률과 일치하는가 |
| `token_reuse` | 결제 실패 후 재사용 시도 결과 | 재사용 주문이 401 없이 수행되는가 |
| `pay_result` / `pay_duration` | 결제 결과 카테고리별 건수/응답 시간 | 기존 payment-load와 동일 분류 |

### TPS 적정성 판정 방법

1. S1 실행 중 `pay_result` 분포와 결제 p95를 관찰한다. 토큰 발급 직후 초당 200명이 주문-결제로 밀려들 때 결제가 무너지면(타임아웃·5xx 급증·서킷 OPEN) **발급 속도가 과속**이라는 뜻이다.
2. 기존 `payment-load.js` 기준 결제는 초당 30회 수준에서 검증되었고 PG 콜백이 1~5초 걸리므로, 초당 200명 발급은 과속일 가능성이 높다. 그 경우 배치 크기(20명/100ms)를 낮추거나 간격을 늘려 다운스트림 최대 TPS 이하로 역산해 조정한다.

## 5. 실행 방법

```shell
# 사전 준비: infra + monitoring + pg-simulator + commerce-api 기동 (k6/README.md 참고)

# 권장: 실행기 사용 — redis-exporter 를 테스트 동안만 띄우고 k6 메트릭을 remote-write
./k6/run-waiting-queue.sh                                    # S1: 10,000명 / 30초 ramp
./k6/run-waiting-queue.sh -e USERS=1000                      # 축소 리허설
./k6/run-waiting-queue.sh -e SCENARIO=saturation -e RATE=300 # S2

# 모니터링 없이 k6 콘솔 요약만
k6 run k6/waiting-queue-load.js
```

주요 env: `USERS`(기본 10000), `PRODUCTS`(기본 100), `SCENARIO`(spike|saturation), `RAMP`(spike 진입 분산 초, 기본 30, 0=동시), `RATE`(saturation 진입 속도, 기본 300), `MAX_WAIT`(토큰 대기 한도 초, 기본 900), `QUEUE_TIMEOUT`(enter/position 요청 타임아웃, 기본 30s), `BASE_URL`.

### 실시간 모니터링 (부하 테스트 시에만)

`run-waiting-queue.sh`가 `redis-exporter`(monitoring-compose의 `loadtest` profile)를 테스트 동안만 띄우고 종료 시 내린다. Grafana → 폴더 `Loopers` → **WaitingQueue · 대기열 부하 테스트** 대시보드에서 확인:

| 패널 | 소스 | 의미 |
| ---- | ---- | ---- |
| 대기열 길이 | `redis_key_size{key="waiting-queue"}` (ZCARD) | 유입>발급 구간에서 자라고 배출되며 감소하는 곡선 |
| Entry-Token 수 | `redis_keys_count{key="entry-token:*"}` (SCAN) | 발급됐지만 미소비·미만료 토큰 수 |
| enter/position p95, 대기 시간 p95, 결제 결과, 흐름 결과 | k6 remote-write | 테스트 관점 지표 |

## 6. 결과 기록

### 1차 실행 (2026-07-10, ramp 도입 전)

| 항목 | 리허설 S1 (USERS=500, 동시 진입) | 풀 스케일 S1 (USERS=10,000, 동시 진입) |
| ---- | ---- | ---- |
| 완주 | 500/500 (WAIT_TIMEOUT 0) | **46/10,000 — 서버 접속 붕괴로 중단** |
| enter p95 | 11.49s (기준 1s 초과) | 측정 불가 (요청/연결 타임아웃 폭주) |
| position p95 | 10.13s (기준 1s 초과) | 측정 불가 |
| queue_wait_time p95 | 11.16s (이론값 2.5s) | - |
| 결제 결과 | **PG_REQUEST_FAILED 855/893 (96%)**, SUCCESS 9, PENDING 26 | - |
| 토큰 소비 | TOKEN_CONSUMED 0 / ON_RETRY 122 / NOT_CONSUMED 310 | - |
| token_reuse | 461회 (재사용 주문 401 거부 0건 — 실패 시 토큰 유지 정책 정상 동작) | - |
| 발급 TPS 판정 | **초당 200명 발급은 명백한 과속.** PG 시뮬레이터 기본 실패율 40%를 훨씬 넘는 96%가 PG 요청 단계에서 실패했고, 결제 평균 30초·최대 59초로 Tomcat 스레드(max 200)가 결제 콜백 대기에 점유되면서 enter/position까지 연쇄 지연됐다. | 동시 10,000 진입은 접속 계층에서 붕괴 → spike 를 30초 ramp 로 변경 |

**1차 결론**
1. 배치 20명/100ms(200/s)는 다운스트림(주문-결제) 처리량을 크게 초과한다. PG 콜백 1~5초 기준 결제 처리량은 초당 수십 건 수준이므로 배치 크기 축소(예: 2~5명/100ms) 또는 간격 확대가 필요하다.
2. 결제 실패 → 동일 토큰 재주문(S3) 경로는 부하 중에도 정상 동작했다 (재사용 주문 401 거부 0건).
3. enter/position 지연의 주 원인은 대기열(Redis)이 아니라 결제 블로킹의 스레드풀 점유 + 요청당 BCrypt 인증이다 (7-1 참고).

### 2차 실행 (2026-07-10, ramp 30s, redis_exporter + Grafana 모니터링)

S1 (USERS=10,000, RAMP=30) — 11분 38초, 10,000 iteration 전원 종료.

| 항목 | S1 (spike, RAMP=30) | S2 (saturation, RATE=300) |
| ---- | ---- | ---- |
| 완주 | 10,000/10,000 (중단 없음) | 미실행 |
| enter | **실패 7,550/10,000 (75.5%)** — 30초 타임아웃. p95 31.5s | - |
| position | p95 30.41s, POSITION_ERROR 1,827 | - |
| queue_wait_time p95 | 28.25s (진입 성공자 기준) | - |
| 대기열 최대 길이 (Redis ZCARD 실측) | **412** — 대기열은 부하를 받지도 못함 | - |
| 동시 Entry-Token 최대 (실측) | **7,493** — 발급됐지만 소비 주체가 없어 TTL 만료 대기 | - |
| 결제 결과 | **SUCCESS 0**, PENDING 186, PG_REQUEST_FAILED 368, 나머지 타임아웃. 결제 평균 45.9s | - |
| 토큰 소비 | TOKEN_CONSUMED 0 / ON_RETRY 34 / NOT_CONSUMED 422 | - |
| token_reuse | 499회 (재사용 주문 401 거부 13건 — TTL 만료 추정) | - |

**2차 결론 — 병목은 대기열이 아니라 인증(BCrypt)이다**

1. **BCrypt가 시스템 전체 처리량을 초당 ~46건으로 캡핑한다.** 시드 단계에서 실측한 가입 속도(초당 46명)가 곧 서버의 BCrypt 처리 용량이고, enter·position·주문·결제 모든 요청이 요청마다 BCrypt 검증을 거친다. 초당 333명 진입 + 폴링 수천 건이 이 좁은 관문에 몰리면서 75.5%가 enter 30초 타임아웃으로 탈락했다.
2. **클라이언트 타임아웃 ≠ 서버 거절.** k6 가 enter 실패로 집계한 유저 상당수가 서버 측에서는 뒤늦게 ZADD 처리됐다 — 진입 성공 집계(~2,450)보다 훨씬 많은 동시 토큰 7,493개가 그 증거다. 유저는 떠났는데 서버는 계속 발급해 토큰이 TTL 만료만 기다리며 쌓였다.
3. **대기열(Redis)은 한 번도 스트레스를 받지 않았다.** 최대 길이 412 — 유효 유입(BCrypt 캡 ~46/s)이 발급 속도(200/s)에 한참 못 미쳐, 대기열은 항상 거의 비어 있었다. 즉 현 구성에서 "처리량 초과" 방어벽은 대기열이 아니라 의도치 않게 인증 계층이 되어 버렸다.
4. **결제는 즉시 SUCCESS 0건.** 발급 속도를 논하기 전에, 대기열 진입·폴링 경로에서 BCrypt 인증을 제거(진입 시 1회 인증 후 세션/서명 토큰)하지 않으면 어떤 발급 TPS 도 의미 있게 측정할 수 없다.

**후속 액션 제안**
- (필수) 대기열 진입·폴링 경로의 요청당 BCrypt 제거 — 1회 인증 후 세션/서명 토큰 방식
- 발급 배치 축소 (1차 결론: 다운스트림 결제 처리량은 초당 수십 건 수준)
- 위 개선 후 S1/S2 재측정

### 3차 실행 (2026-07-10, ramp 30s + 대기열 무인증)

2차 직후 대기열 경로 무인증 전환(요구사항 5-1-1)을 적용하고 재측정했다.
S1 (USERS=10,000, RAMP=30) — 16분 13초, 10,000명 전원 완주.

| 항목 | 값 | 해석 |
| ---- | ---- | ---- |
| 진입 | 성공 8,124 / ENTER_FAILED 1,876 (2차 7,550 대비 -75%) | 무인증 전환 효과. 잔여 실패는 다운스트림 혼잡의 2차 피해 (결론 2) |
| 대기열 피크 (Redis 실측) | **4,480명** | 이론값 (유입 333/s − 배출 200/s) × 30s ≈ 4,000 과 부합 — **대기열이 처음으로 설계대로 동작함을 검증** |
| queue_wait_time | 중앙값 49.5s (이론값 부합) / p95 4m50s | 중앙값은 설계대로. p95 꼬리는 서버 혼잡(스레드 고갈) 구간의 지연 |
| enter / position p95 | 19.45s / 30.06s | 대기열 로직이 아니라 **Tomcat 스레드 고갈**: 결제가 콜백 대기(평균 40.9s)로 스레드를 점유해 가벼운 대기열 요청까지 큐잉됨 |
| 주문 | ORDER_FAILED 4,675 / **ORDER_REJECTED_401 1,394 (토큰 TTL 만료)** | 발급 200/s 대비 다운스트림 완료 ~2건/s → 토큰 보유자가 주문·결제 단계에서 수 분 대기, 상당수 5분 TTL 초과 |
| 동시 토큰 피크 (실측) | **7,734개** | 발급 속도 ≫ 소비 속도의 증거 |
| 결제 | **SUCCESS 142 (최초 성공 확인)**, CIRCUIT_OPEN 259 (서킷브레이커 최초 동작), PG_REQUEST_FAILED 609 | 전체 여정(진입→발급→주문→결제→토큰삭제) 최초 검증: TOKEN_CONSUMED 68 + ON_RETRY 1 |
| token_reuse | 1,740회 | 실패 시 토큰 유지 정책 부하 중에도 정상 동작 |

**3차 결론**

1. **무인증 전환은 성공.** 대기열이 이론값대로 쌓이고(피크 4,480 ≈ 계산치 4,000) 대기 시간 중앙값(49.5s)도 설계값과 일치 — 대기열 자체(Redis ZSET + 스케줄러 배출)는 초당 333명 유입을 문제없이 처리했다.
2. **발급 20명/100ms(초당 200명)는 확정적 과속.** 다운스트림(주문-결제) 실측 완료 처리량은 초당 ~2건. 과잉 발급의 피해가 세 형태로 나타났다: ① 토큰 1,394개 TTL 만료(주문 기회 상실) ② 결제 콜백 대기가 Tomcat 스레드를 점유해 대기열 API까지 연쇄 지연(position p95 30s) ③ 서킷브레이커 OPEN 259건.
3. **권장 발급 속도**: 결제 파이프라인(주문+결제 BCrypt 2회, PG 콜백 1~5s)의 안정 처리량을 근거로 **배치 2~3명/100ms (초당 20~30명)** 로 축소 후 재측정. 이 수준이면 토큰 TTL(5분) 내 소비도 자연 보장된다.

### 4차 실행 (2026-07-10, 배치 2명/100ms + 폴링 5/3/1초 — ADR-041 적용)

S1 (USERS=10,000, RAMP=30) — 20분 7초, 10,000명 전원 종료.

| 지표 | P50 | P90 | P95 | 비고 |
| ---- | ---- | ---- | ---- | ---- |
| enter | **552ms** | 13.01s | 18.2s | 중앙값 최초 sub-second (3차 med 6.05s) |
| position | 9.4s | 29.32s | 30s | 30s = 타임아웃 상한에 박힘 — 여전히 스레드 고갈 |
| queue_wait_time (진입→발급) | 15.59s | 10m35s | 10m44s | 이론 상한 ~7.5분(8,936명/20명s) 초과 — 혼잡 구간 지연 |
| journey_time (진입→결제완료) | 11m53s | 12m3s | 12m4s | 완주자 182명 기준 종단 여정 |
| pay_duration | 47.3s | 1m0s | 1m0s | 결제 자체가 아니라 스레드 큐잉 대기 |

| 항목 | 값 | 해석 |
| ---- | ---- | ---- |
| 흐름 결과 | TOKEN_CONSUMED 162 + ON_RETRY 20 (3차 69 대비 2.6배) / **POSITION_ERROR 6,172** / ENTER_FAILED 1,064 / ORDER_REJECTED_401 904 | 완주는 늘었지만 폴링 타임아웃 탈락이 최대 실패 요인 |
| 대기열 피크 (실측) | 2,835 → 완전 배출 | 곡선 자체는 정상 (쌓였다 일정 기울기로 소진) |
| 동시 토큰 피크 (실측) | **5,856 ≈ 발급 20/s × TTL 300s = 6,000** | 발급 페이스가 설계값(20/s)대로 동작함을 실측으로 검증 |
| 결제 | SUCCESS 72, PG_REQUEST_FAILED 1,637, CIRCUIT_OPEN 119 | PG 40% 실패 + 타임아웃이 여전히 지배 |
| 서버 총 처리량 | **47 req/s** (http_reqs 평균) | 시스템 전체가 BCrypt·스레드 상한에 캡핑 |

**4차 결론 — 20/s 발급은 "딱 상한"이라 여유가 없다**

1. **개선 확인**: enter 중앙값 552ms(무인증 + 배치 축소 효과), 대기열 곡선 정상, 발급 페이스 실측 검증(토큰 피크 ≈ 이론값), 완주 2.6배.
2. **남은 병목은 결제의 동기-블로킹 설계**: 결제 1건이 콜백 대기(`pg.callback-timeout-seconds=10s`)로 Tomcat 스레드 1개를 최대 10초 점유한다. 즉 지속 가능한 결제 유입 상한 = 200 스레드 ÷ 10s = **초당 20건 — 발급 20/s 와 정확히 같아 여유가 0**이다. 여기에 재시도(token_reuse 2,072)와 PENDING 소비 확인 폴링이 겹치면 즉시 초과되어, position 폴링(6,172 실패)까지 스레드 큐에 갇힌다.
3. **후속 옵션** (우선순위 순):
   - 배치 1명/100ms(10/s)로 추가 축소 — 스레드 점유 ~50%, 재시도 여유 확보. 상수 하나로 즉시 가능
   - (근본) 결제 API 의 콜백 동기 대기 제거 — 즉시 PENDING 반환 + 클라이언트 폴링/웹훅. 실서비스 설계이나 본 주차 범위 초과
   - 대기열 경로 전용 커넥터/스레드풀 분리 — 오버엔지니어링 후보로 백로그

### 5차 실행 (2026-07-10, USERS=2,000 — 서버 1대 수용 규모로 축소)

S1 (USERS=2,000, RAMP=30, 배치 2명/100ms) — 5분 17초, 2,000명 전원 종료.

| 지표 | P50 | P90 | P95 | P99 | 비고 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| enter | **14.8ms** | 69ms | **99ms** | 194ms | **SLO(p95<1s) 최초 달성**, max 671ms |
| position | 72ms | 6.04s | 7.24s | 14.1s | 중앙값 정상, 꼬리는 결제 버스트 구간 |
| queue_wait_time (진입→발급) | 44.5s | 1m25s | 1m27s | 1m29s | 이론값(2,000명/20명s = 100s)과 정확히 부합 |

| 항목 | 값 | 해석 |
| ---- | ---- | ---- |
| 대기열 실패 | **전무** — ENTER_FAILED 0 / WAIT_TIMEOUT 0 / ORDER_FAILED 0 / **ORDER_REJECTED_401(TTL 만료) 0** | 전원이 토큰을 받아 TTL 내 주문까지 도달 |
| 대기열 피크 (실측) | 1,421 ≈ 이론값 1,400 | 유입 67/s − 배출 20/s × 30s |
| k6 관점 결제 | SUCCESS 0, TOKEN_NOT_CONSUMED 1,971 | **측정 창(20초)의 한계 — 아래 참조** |
| **DB 관점 결제 (실측)** | **SUCCESS 1,464 / FAILED 659 / PENDING 1,903** (시도 총합, 재시도 포함) | 결제는 실제로 성공하고 토큰 소비 체인도 동작 — 단지 늦게 |
| 서버 총 처리량 | 240 req/s (http_req_failed 4.5%) | 대기열 무인증 경로가 처리량을 견인 |

**5차 결론 — 대기열은 완결 검증, 남은 것은 결제 테스트 더블의 콜백 지연**

1. **대기열 파트 최종 판정: 합격.** 진입 p95 99ms(SLO 달성), 실패 0, 대기 시간이 이론값과 오차 없이 일치, TTL 만료 0. **서버 1대 기준 "2,000명 / 발급 20명/초"가 검증된 수용 규모**다.
2. **k6 의 "SUCCESS 0" 은 측정 한계다.** DB 확인 결과 1,464건이 SUCCESS 로 확정됐고 토큰 소비 체인도 동작했다. 원인: pg-simulator 가 초당 12건+ 버스트에서 콜백 백로그를 만들어 **콜백이 수 분 지연** — k6 의 토큰 소비 판정 창(20초)을 훌쩍 넘겼다 (테스트 종료 후에도 콜백 유입이 관측됨). 지연 콜백 51건은 유효성 검증에서 "결제 정보를 찾을 수 없습니다"로 거부되기도 했다.
3. **교훈**: 비동기 콜백 기반 결제의 부하 테스트에서는 "즉시 응답"이 아니라 **최종 일관성 도달 시간**을 측정해야 한다. k6 소비 판정 창을 늘리는 대신, DB 상태를 사후 검증하는 방식이 정확했다.

### 6차 실행 (2026-07-10, 배치 1명/100ms — ADR-041 후속 조정 적용)

S1 (USERS=2,000, RAMP=30) — 6분 39초, 2,000명 전원 종료.

| 지표 | P50 | P90 | P95 | P99 | 5차 대비 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| enter | 4.8ms | 35ms | **60ms** | 136ms | 99ms → 60ms |
| position | 68ms | 2.73s | **4.0s** | 6.78s | 7.24s → 4.0s (여전히 SLO 초과, 결제 구간 꼬리) |
| queue_wait_time (진입→발급) | 1m53s | 2m48s | 3m1s | 3m6s | 이론값(2,000/10 = 200s)과 부합 |
| **journey_time (진입→결제완료)** | 51.6s | 1m55s | **1m57s** | 2m21s | 5차 측정 불가(0건) → **77건 측정 성공** |

| 항목 | 값 | 해석 |
| ---- | ---- | ---- |
| 대기열 실패 | ENTER_FAILED 0 / WAIT_TIMEOUT 0 / ORDER_FAILED 0 / 401 5건 | 대기열 파트는 5차와 동일하게 안정 |
| 대기열 피크 (실측) | 1,686 ≈ 이론값 (유입 67/s − 배출 10/s) × 30s | 설계대로 |
| 토큰 소비 (k6 20초 창 내 확인) | **77건 (5차 0건)** | 발급 10/s 로 콜백 처리가 일부 따라잡기 시작 |
| DB 관점 결제 (실측) | SUCCESS 555 / PENDING 1,473 / FAILED 259 | 콜백 백로그는 여전 (PENDING 다수) |
| k6 결제 분류 | PG_REQUEST_FAILED 3,127/3,869 (**81%**) | ⚠ pg-simulator 열화 — 아래 참조 |
| 서버 총 처리량 | 359 req/s (실패율 2.3%) | 5차 240 req/s 대비 여유 증가 |

**6차 결론**

1. **발급 10/s 의 효과는 명확하다**: position p95 7.2s → 4.0s, k6 창 내 토큰 소비 0 → 77건, journey_time 최초 측정(p95 1m57s). 스레드 여유가 생기면서 결제 버스트 구간의 연쇄 지연이 완화됐다.
2. **position 이 여전히 SLO(1s)를 넘는 것과 PG 요청 실패 81%는 pg-simulator 열화가 주 원인으로 보인다.** pg-simulator 는 수 시간 동안 재기동 없이 수만 건의 결제 레코드를 쌓아왔고, 요청 처리·콜백 발송이 갈수록 느려졌다 (설계 실패율 40% ≪ 관측 81%). 공정한 최종 비교를 위해서는 pg-simulator 를 재기동한 클린 환경 재측정이 필요하다.
3. 대기 시간(p95 3분)은 발급 속도를 절반으로 낮춘 의도된 대가다. 2,000명 규모에서 토큰 TTL(5분) 내 소비는 여전히 보장된다 (401 5건 = 0.25%).

### 7차 실행 (2026-07-10, 6차와 동일 조건 + pg-simulator 재기동) — 최종

S1 (USERS=2,000, RAMP=30, 배치 1명/100ms) — 6분 34초, 2,000명 전원 종료.

| 지표 | P50 | P90 | P95 | P99 | 6차 대비 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| enter | 4.7ms | 40ms | **62ms** | 108ms | 동일 (안정) |
| position | 27ms | 1.69s | **2.54s** | 3.6s | 4.0s → 2.54s |
| queue_wait_time | 1m34s | 2m39s | 2m52s | 2m55s | 이론값 부합 (동일) |
| journey_time (진입→결제완료) | 42.8s | 51.2s | **53.7s** | 54s | 1m57s → 53.7s (절반) |

| 항목 | 값 | 해석 |
| ---- | ---- | ---- |
| 대기열 실패 | ENTER_FAILED 0 / WAIT_TIMEOUT 0 / ORDER_FAILED 0 / **401(TTL 만료) 0** / **CIRCUIT_OPEN 0** | 최초로 전 카테고리 무결점 |
| 토큰 소비 (k6 20초 창 내) | 94건 (6차 77) | |
| DB 관점 결제 | SUCCESS 600 / PENDING 1,447 / FAILED 275 | 콜백 지연은 여전히 존재 (테스트 더블 한계) |
| 서버 총 처리량 | 412 req/s (실패율 1.7%) | 시리즈 최고 |

**7차 결론 (시리즈 최종)**

1. **대기열 최종 판정 — 완결.** 7회 실행을 통해 확정된 권고 구성: **서버 1대 / 유저 2,000명 / 발급 1명/100ms(초당 10명) / 폴링 5·3·1초 / 대기열 무인증**. 이 구성에서 진입 p95 62ms, 대기 시간은 이론값과 일치, TTL 만료·서킷 OPEN·대기열 실패 전부 0.
2. **position p95 2.54s (SLO 1s 초과)의 잔여 원인은 결제의 동기-블로킹**: PENDING 응답의 실측 스레드 점유가 30~47초에 달해(콜백 대기), 실효 결제 처리 용량은 초당 ~6건 수준. 발급 10/s 도 이보다 크므로 결제 버스트 구간의 스레드 경합이 완전히는 사라지지 않는다. **해소하려면 결제 API 비동기화(즉시 PENDING 반환)가 필수** — 백로그 1순위.
3. PG 요청 실패율(70%)이 재기동 후에도 설계값(40%)을 웃도는 것 역시 같은 원인(pg-simulator 의 동기 처리 용량 초과)으로, 테스트 더블의 한계로 기록한다.

### 8차 실행 (2026-07-10, 전체 서비스 재기동 후 재현성 확인 + 토큰 생명주기 관측)

S1 (USERS=2,000, RAMP=30, 배치 1명/100ms) — 6분 21초, 2,000명 전원 종료. commerce-api·pg-simulator 모두 fresh 기동.

| 지표 | P95 | 7차 | 판정 |
| ---- | ---- | ---- | ---- |
| enter | 63ms | 62ms | **재현** |
| position | 3.94s | 2.54s | 2.5~4s 변동 범위 내 (원인 동일: 결제 스레드 점유) |
| queue_wait_time | 2m51s | 2m52s | **재현** (이론값 부합) |
| journey_time | 58.9s | 53.7s | **재현** |
| 실패 카테고리 | 진입/타임아웃/401/서킷 모두 0 | 동일 | **재현** |

**토큰 생명주기 실측 (exporter 5분 유지 개선 적용 후 최초 관측)**

```
12:51 →  40   발급 시작 (10/s 선형 증가)
12:55 → 1,880 피크
12:56 → 1,830 결제 소비(DEL) 시작으로 꺾임
12:59 →  271  소비 + TTL 만료 가속
13:00 →    0  전량 소멸 (테스트 종료 +4분)
```

- DB 실측: **SUCCESS 1,107** (7차 600 대비 — fresh pg-simulator 가 콜백을 더 잘 소화) / FAILED 472 / PENDING 718 (건수는 재시도 포함).
- 약 55%는 결제 성공으로 소모(리스너 DEL), 나머지는 TTL 만료 — 토큰 잔존·누수 없음 확인.
- k6 의 PG_REQUEST_FAILED(90%) 같은 즉시-응답 분류는 비동기 PG 특성상 과대집계되므로, 결제 성공률 판정은 DB 를 기준으로 한다 (5차 교훈 동일).

## 7. 주의사항 (결과 해석 시)

1. **BCrypt 병목 주의**: `AuthInterceptor`가 모든 요청(폴링 포함)에 BCrypt 검증을 수행한다. 폴링 수천 RPS × BCrypt는 서버 CPU를 지배할 수 있으므로, "대기열이 느리다"로 오독하지 말 것. 이는 대기열 폴링 경로에 비밀번호 인증을 태우지 않는 설계 개선 포인트이기도 하다.
2. **waitingCount는 스냅샷**: ZADD와 ZCARD 사이에 스케줄러 ZPOPMIN이 끼어들 수 있어 강한 일관성이 없다 (요구사항 5-1).
3. **토큰 삭제는 비동기 fire-and-forget**: 결제 성공 → 토큰 삭제는 AFTER_COMMIT 비동기 리스너이므로 판정에 폴링 유예(최대 20초)를 둔다.
4. **PENDING 판정 한계**: 결제 응답이 PENDING이면 콜백 결과를 직접 알 수 없어, "position 404 = 성공" / "20초 후에도 토큰 잔존 = 실패"로 간접 판정한다. PG 실패율 40%만큼의 토큰 잔존은 정상이다.
